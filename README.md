
  # Landing page with modes

  This is a code bundle for Landing page with modes. The original project is available at https://www.figma.com/design/aSaaQ3W2uAflJyxZ5k5QdA/Landing-page-with-modes.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  