# Meta Holding frontend

React/Vite public website for Meta Holding.

Project albums, project fields, categories, regions, and Nezarat table rows are loaded from the FastAPI API. The frontend does not contain an admin panel. `/admin` redirects to the backend SQLAdmin URL configured by `VITE_ADMIN_URL`.
